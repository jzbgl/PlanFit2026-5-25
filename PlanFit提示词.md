请帮我生成一个 Tauri + React + TypeScript + TailwindCSS 桌面应用提示词。

应用名称：PlanFit
功能需求：

1. 左侧边栏导航：今日计划 / 计划概览 / 训练记录 / 我的
2. 今日计划页面：展示当天的训练动作列表（动作名称、组数、次数、休息秒数），每个动作右侧有"完成"复选框，全部完成后显示庆祝动画。
3. 计划概览页面：显示一个为期4周的训练日历（表格形式），标记已完成的训练日。
4. 训练记录页面：用图表（Chart.js 或 Recharts）显示每周训练次数趋势。
5. 我的页面：显示用户信息（姓名、身高、体重、健身目标），支持编辑。
6. 所有数据保存在本地（使用 IndexedDB 或 localStorage）。
7. UI 风格：深色主题、卡片圆角、主色调 #00E676。
